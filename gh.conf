## configuration begin ##

## Edit this file using your own github repos wanting to push to
## Make sure and change _repo_count variable to reflect the count of your repos

# path to your local github directory
dev_directory="$HOME/github/"      # usually $HOME/github

# number of YOUR github repos (default 10)
_repo_count=11

# YOUR github user (if use multiple users edit each repo 'user' below)
github_user="idk"

###  format for repos are
#_repo1[1]="your_username_on_github"    # user
#_repo1[2]="your_repository_on_github"  # repository
#_repo1[3]=""                           # custom commands (if any)

### the following are my personal github repos
### these will not work for you but are includes as examples

# github repo Number 1
_repo1[1]=${_github_user}  # user
_repo1[2]="pdq"  # repository
# custom commands
 _repo1[3]="cp $HOME/.xinitrc ${dev_directory}${_repo1[2]}/.xinitrc ; cp $HOME/.bashrc ${dev_directory}${_repo1[2]}/.bashrc ; cp $HOME/.bash_profile ${dev_directory}${_repo1[2]}/.bash_profile ; cp $HOME/.dmenu_cache ${dev_directory}${_repo1[2]}/.dmenu_cache ; cp $HOME/.nanorc ${dev_directory}${_repo1[2]}/.nanorc ; cp $HOME/.gtkrc-2.0 ${dev_directory}${_repo1[2]}/.gtkrc-2.0 ; mkdir -p ${dev_directory}${_repo1[2]}/.config/spacefm ; cp $HOME/.config/spacefm/bookmarks ${dev_directory}${_repo1[2]}/.config/spacefm/bookmarks ; mkdir -p ${dev_directory}${_repo1[2]}/.config/fontconfig ; cp $HOME/.config/fontconfig/fonts.conf ${dev_directory}${_repo1[2]}/.config/fontconfig/fonts.conf ; mkdir -p ${dev_directory}${_repo1[2]}/.vimperator/plugin ; cp -r $HOME/.vimperator/plugin/. ${dev_directory}${_repo1[2]}/.vimperator/plugin ; mkdir -p ${dev_directory}${_repo1[2]}/.config/htop ; cp $HOME/.config/htop/htoprc ${dev_directory}${_repo1[2]}/.config/htop/htoprc ; mkdir -p ${dev_directory}${_repo1[2]}/.config/pacaur ; cp $HOME/.config/pacaur/config ${dev_directory}${_repo1[2]}/.config/pacaur/config ; mkdir -p ${dev_directory}${_repo1[2]}/.config/parcellite ; cp $HOME/.config/parcellite/parcelliterc ${dev_directory}${_repo1[2]}/.config/parcellite/parcelliterc ; mkdir -p ${dev_directory}${_repo1[2]}/.config/transmission-daemon ; cp $HOME/.config/transmission-daemon/settings.json ${dev_directory}${_repo1[2]}/.config/transmission-daemon/settings.json ; mkdir -p ${dev_directory}${_repo1[2]}/.moc/themes ; cp -r $HOME/.moc/themes/. ${dev_directory}${_repo1[2]}/.moc/themes ; cp -r $HOME/.moc/config ${dev_directory}${_repo1[2]}/.moc/config ; mkdir -p ${dev_directory}${_repo1[2]}/.kde4 ; cp -r $HOME/.kde4/share/config/dolphinrc ${dev_directory}${_repo1[2]}/.kde4/dolphinrc ; mkdir -p ${dev_directory}${_repo1[2]}/.config/nitrogen ; cp -r $HOME/.config/nitrogen/. ${dev_directory}${_repo1[2]}/.config/nitrogen ; mkdir -p ${dev_directory}${_repo1[2]}/.kde4 ; cp -r $HOME/.kde4/share/apps/dolphin/dolphinui.rc ${dev_directory}${_repo1[2]}/.kde4/dolphinui.rc ; $HOME/bin/backup_pkgs"
# github repo Number 2
_repo2[1]=${_github_user}  # user
_repo2[2]="eggdrop-scripts"  # repository
_repo2[3]="cp -r $HOME/.eggdrop/scripts/custom/ ${dev_directory}${_repo2[2]}"

# github repo Number 3
_repo3[1]=${_github_user}  # user
_repo3[2]="zsh"  # repository
_repo3[3]="cp $HOME/.zshrc ${dev_directory}${_repo3[2]}/.zshrc ; cp $HOME/.zprofile ${dev_directory}${_repo3[2]}/.zprofile ; cp -r $HOME/.zsh ${dev_directory}${_repo3[2]}"

# github repo Number 4
_repo4[1]=${_github_user}  # user
_repo4[2]="awesomewm-X"  # repository
_repo4[3]="cp -r $HOME/.config/awesome/. ${dev_directory}${_repo4[2]}"

# github repo Number 5
_repo5[1]=${_github_user}  # user
_repo5[2]="conky-X"  # repository
_repo5[3]="cp -r $HOME/.config/conky/. ${dev_directory}${_repo5[2]}"

# github repo Number 6
_repo6[1]=${_github_user}  # user
_repo6[2]="bin"  # repository
_repo6[3]="cp -r $HOME/bin/. ${dev_directory}${_repo6[2]} ; cp /usr/bin/screenfetch ${dev_directory}${_repo6[2]}/screenfetch"

# github repo Number 7
_repo7[1]=${_github_user}  # user
_repo7[2]="etc"  # repository
_repo7[3]="cp /etc/modules-load.d/my_modules.conf ${dev_directory}${_repo7[2]}/my_modules.conf ; cp /etc/php/php.ini ${dev_directory}${_repo7[2]}/php.ini ; cp /etc/tor/torrc ${dev_directory}${_repo7[2]}/torrc ; cp /etc/pacman.conf ${dev_directory}${_repo7[2]}/pacman.conf ; cp /etc/pacman.d/mirrorlist ${dev_directory}${_repo7[2]}/mirrorlist ; cp /etc/X11/xorg.conf.d/custom.conf ${dev_directory}${_repo7[2]}/custom.conf"

# github repo Number 8
_repo8[1]=${_github_user}  # user
_repo8[2]="systemd"  # repository
_repo8[3]="cp -r /etc/systemd/system/* ${dev_directory}${_repo8[2]}"

# github repo Number 9
_repo9[1]=${_github_user}  # user
_repo9[2]="php"  # repository
_repo9[3]="cp -r $HOME/php/* ${dev_directory}${_repo9[2]}"

# github repo Number 10
_repo10[1]=${_github_user}  # user
_repo10[2]="gh"  # repository
_repo10[3]="cp -r /usr/bin/gh ${dev_directory}${_repo10[2]}/gh ; cp -r $HOME/.config/gh/gh.conf ${dev_directory}${_repo10[2]}/gh.conf"

# github repo Number 10
_repo11[1]=${_github_user}  # user
_repo11[2]="pdq-utils"  # repository
_repo11[3]="cp -r /usr/bin/pdq ${dev_directory}${_repo11[2]}/pdq ; cp -r /usr/bin/pdql ${dev_directory}${_repo11[2]}/pdq-light"

## github repo Number 12
# _repo12[1]=${_github_user}  # user
# _repo12[2]="luakit-X"  # repository
# _repo12[3]="cp -r $HOME/.config/luakit/. ${dev_directory}${_repo12[2]}"

## configuration end ##